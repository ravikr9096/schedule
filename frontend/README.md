## Frontend (React + Vite)

### Prereqs

- Install Node.js (includes `npm`).

### Setup

```bash
npm install
```

### Run (dev)

```bash
npm run dev
```

This dev server proxies `/api/*` and `/health` to `http://127.0.0.1:8000`.

